import { MainNavButtonDirective } from './main-nav-button.directive';

describe('MainNavButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MainNavButtonDirective();
    expect(directive).toBeTruthy();
  });
});
